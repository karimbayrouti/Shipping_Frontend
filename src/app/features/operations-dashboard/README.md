# Operations Dashboard

The administrative portal landing page, transcribed from
`Shipping/prototype-html/pages/dashboard-operations/index.html` and the
prototype `OperationsDashboard` component. It intentionally preserves the
prototype markup and static demonstration state. Interactive destinations are
enabled as their owning screens are migrated.
